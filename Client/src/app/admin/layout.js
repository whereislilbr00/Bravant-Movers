export const metadata = {
  title: 'Admin Dashboard - Bravant Movers',
  description: 'Manage bookings, payments, and customer inquiries',
};

export default function AdminLayout({ children }) {
  return children;
}
